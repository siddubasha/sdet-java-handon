package com.nonterminalandterminal.in;

public class Introduction {
   /*
     In stream we are having two types of methods.
     
    1 . Non Terminal methods   ---->    filter,map,flatMap,distinct,limit ( applied on streams)
    2 . Terminal methods    ------>     collect,count,max,min,forEach,toArray ( applied on objects of stream i.e after using of non terminal method) 
    
            we can use terminal methods directly without non terminal methods also.
    
    
    */
}
